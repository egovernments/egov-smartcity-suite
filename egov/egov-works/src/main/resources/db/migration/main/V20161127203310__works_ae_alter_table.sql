ALTER TABLE EGW_ABSTRACTESTIMATE ADD COLUMN COPIED_FROM bigint;
ALTER TABLE EGW_ABSTRACTESTIMATE ADD CONSTRAINT fk_copied_from FOREIGN KEY (copied_from)
      REFERENCES egw_abstractestimate (id);

--rollback ALTER TABLE EGW_ABSTRACTESTIMATE DROP COLUMN COPIED_FROM;